import React from "react";
import { AppBar, Toolbar, Typography } from "@mui/material";

function MainNevigation() {
  return (
    <AppBar position="static">
      <Toolbar>
        <Typography variant="h6">Hyphen</Typography>
      </Toolbar>
    </AppBar>
  );
}
export default MainNevigation;
